# Architecture

This folder documents the lab architecture:
- Nested virtualization: VMware → Proxmox VE
- Guest: Ubuntu Server VM
- Docker lab: nginx + postgres + adminer on a custom bridge network (`lab-net`)

See: `diagram.md` for the Mermaid diagram.



